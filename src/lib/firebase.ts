// src/lib/firebase.ts
// This file is intentionally left almost empty as Firebase client SDK is removed.
// If any other Firebase service (e.g., Firestore, Storage client-side) were to be used
// without Auth, its initialization would go here. For now, it's not needed.

// console.log("Firebase client setup has been removed for local-only MongoDB usage.");

// To completely remove, you can delete this file if no other Firebase client services are planned.
// For now, leaving it as a marker of removal.
export {};
